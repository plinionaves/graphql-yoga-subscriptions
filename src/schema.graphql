type Query {
  posts: [Post!]!
}

type Mutation {
  createPost(input: PostInput): Post!
}

type Post {
  id: Int!
  title: String!
  author: String!
}

input PostInput {
  title: String!
  author: String!
}